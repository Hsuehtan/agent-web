/**
 * OEM 配置文件 — 品牌展示定制的唯一入口
 *
 * 使用方法：
 *   1. 修改下方各项的值，替换为你的品牌信息。
 *   2. 保存文件后，开发服务器会自动热更新（HMR），无需重启。
 *   3. 无需修改任何源码即可完成品牌展示定制。
 *
 * 注意：本文件仅覆盖"类别1"配置项（静态展示 / 纯文案）。
 *       "类别2"项（包名、数据库路径、JWT、HTTP Header 等）不在本文件中配置，
 *       因为它们会影响代码逻辑，需谨慎迁移后再修改。
 */

const oemConfig = {

  // ─── 品牌标识 ────────────────────────────────────────────────
  brand: {
    /** 品牌名称：显示在侧边栏 Logo 文字、登录页 alt 文本，以及 i18n 全局品牌名替换 */
    name: '测试账号',

    /** 登录页标题：覆盖 i18n 中的 login.title，留空则使用 i18n 默认值 */
    loginTitle: '测试账号',

    /** 浏览器标签页标题（即 <title> 标签内容） */
    pageTitle: '测试账号',

    /** [已废弃] 原版本号前缀，现已由下方 versionText 字段替代 */
    versionPrefix: 'Hsu4',

    /** Logo 图片路径（相对于 packages/client/public/ 目录） */
    logoPath: '/mj-logo.png',

    /** Favicon 图标路径（相对于 packages/client/public/ 目录） */
    faviconPath: '/mj-logo.ico',
  },

  // ─── 外部链接 ────────────────────────────────────────────────
  links: {
    /** 侧边栏底部的 GitHub 仓库链接，设为 '' 则隐藏该链接 */
    github: '',

    /** 侧边栏底部的官网链接，设为 '' 则隐藏该链接 */
    website: '',
  },

  // ─── 模型提供商预设 ──────────────────────────────────────────
  providers: {
    /**
     * 要排除的提供商 value 列表。填入 provider 的 value 字段即可将该提供商从预设列表中移除，
     * 无需修改源码。
     *
     * 示例：
     *   移除 apikey.fun 提供商：['fun-codex', 'fun-claude']
     *   移除 Nous Portal：      ['nous']
     *   全部移除：              ['fun-codex', 'fun-claude', 'nous']
     */
    excludeValues: [] as string[],
  },

  // ─── 版本号外露文案 ──────────────────────────────────────────

  /**
   * 侧边栏底部版本区域显示的文案。
   * 直接展示此文案，不会拼接版本号，不支持点击。
   *
   * 示例：
   *   '测试版系统，演示使用'
   *   'Acme Corp v1.0'
   *   '内部预览版'
   */
  versionText: '测试版系统，演示使用',

  // ─── 登录页提示文案 ──────────────────────────────────────────

  /**
   * 登录页底部提示文案（默认凭据提示）。
   * 设为 '' 则隐藏该提示。
   *
   * 示例：
   *   '默认账号: admin / 123456'
   *   '请联系管理员获取账号'
   *   ''  （隐藏提示）
   */
  loginHintText: '默认账号: admin 密码请联系管理员获取',

  // ─── 空状态 Logo ────────────────────────────────────────────
  emptyState: {
    /** 空状态（无消息时）显示的 Logo 图片路径（相对于 public 目录） */
    logoPath: '/mj-logo.png',
    /** 空状态 Logo 的 alt 文本 */
    logoAlt: '测试账号',
  },

  // ─── 思考动画视频 ────────────────────────────────────────────
  thinkingVideo: {
    /**
     * 亮色模式下思考状态动画视频路径（相对于 public 目录）。
     * 设为 '' 则不显示视频动画。
     */
    light: '/assets/mp4/test_thinking_light.mp4',
    /**
     * 暗色模式下思考状态动画视频路径（相对于 public 目录）。
     * 设为 '' 则不显示视频动画。
     */
    dark: '/assets/mp4/test_thinking_dark.mp4',
  },

  // ─── 联系我们按钮 ──────────────────────────────────────────
  contactUs: {
    /** 按钮文案，设为 '' 则隐藏该按钮 */
    buttonText: '联系我们',

    /** 点击按钮后弹窗中显示的文案 */
    dialogText: '如需正式版本，请联系管理员',

    /** 弹窗中展示的图片 URL（支持相对路径或绝对路径，相对路径基于 public 目录） */
    dialogImage: '/mj-logo.png',
  },

  // ─── 国际化品牌名替换 ────────────────────────────────────────
  i18n: {
    /**
     * i18n 品牌名：设置后，所有翻译文本中出现的 "Hermes" / "hermes"
     * 都会在运行时被替换为此值。
     *
     * 设为 '' 可关闭替换（保留原文中的 "Hermes"）。
     *
     * 注意：替换仅作用于用户可见的文案文本，不影响 API 路径、CLI 命令名、
     * localStorage 键名等逻辑层内容。
     */
    brandName: '测试账号',
  },
}

export type OemConfig = typeof oemConfig
export default oemConfig
